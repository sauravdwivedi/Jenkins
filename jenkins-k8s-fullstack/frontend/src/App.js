import './App.css';
import PostTransactions from './PostTransactions';

export default function App() {
    return (
        <section>
            <h1 className='App-header'>Transaction Management UI</h1>
            <PostTransactions />
        </section>
    );
}